/**
 * FR-17 to FR-19: what makes a Workflow chain valid. One implementation, shared
 * by `POST /api/workflows`, `PUT /api/workflows/<id>` and the Workflow Builder.
 */
export * from './validate.ts'
