# Prose review: prd.md

**Read:** This document exists to help a five-person hackathon team, and the UX, architecture, and epics writers who follow it, build the AgentDesk MVP in three days by fixing what it does in testable terms and in what order to build and cut it.

**Reader:** humans (default), with downstream BMad skills as second readers, so unambiguous defined terms matter more than usual.

**Structure pass:** ran first; its findings are applied in the current text. Nothing below reopens shape.

**Word metrics:** 8,038 words total after the structure edits, from `word_metrics.py`.

**Voice preserved:** terse declarative present tense; sentence fragments in §1 ("A shared contract, so…") and §6 ("Cut from the bottom.") are rhetorical and kept; the house pattern "A total above the budget refuses the Run" (condition as subject) is kept; §1 and §2 use lower-case plain words by design and are not touched; British -ise spelling is the majority form; schema block, tables, and FR identifiers are untouched.

| Pass | Original Text | Revised Text | Changes |
|---|---|---|---|
| prose | FR-27 body: "For `research` and `risk` this is scored as a failed Settlement immediately, with no window [ASSUMPTION: …]. For `data`, `execution`, and `notify` it is logged, the Run stops, and no Slash or Refund occurs in the MVP." | "In every case the Run stops (FR-29). For `research` and `risk` the Call is scored as a failed Settlement immediately, with no window [ASSUMPTION: …]. For `data`, `execution`, and `notify` it is only logged; no Slash or Refund occurs in the MVP." | "the Run stops" sits only in the unscored-Type sentence, so a reader can infer that a paid `research` or `risk` failure leaves the Run running. FR-29 says every failed Node stops the Run. |
| prose | FR-33 consequence 3: "the platform-fetched Binance production public market data"; FR-34 consequence 1: "the production last price at fill time and the lowest (for BUY) or highest (for SELL) production price inside the window"; §11 FR-33 entry: "Binance production public market data" | "the platform-fetched public market data of the live Binance exchange (not Spot Testnet)"; "the live-exchange last price at fill time and the lowest (for BUY) or highest (for SELL) live-exchange price inside the window"; "public market data from the live Binance exchange". Consider: any single word for the live exchange, used in all three places? | FR-33 uses "production mode" for the Settlement mode two lines above, so "production price" reads as "the price used in production mode". Two meanings of one word inside one FR pair. |
| prose | FR-36 body: "recomputes Reputation after every Settlement as passed over scored Calls among the Agent's last 30 scored Calls" | "recomputes Reputation after every Settlement as the share of passed Calls among the Agent's last 30 scored Calls" | "passed over" reads as "skipped". |
| prose | SM-C1: "Share of paid Calls that end `failed_after_payment` or fail Settlement for an unscored Type, where the Builder paid and got nothing back." | Consider: "Share of paid Calls of an unscored Type that end `failed_after_payment`, where the Builder paid and got nothing back."? | Unscored Types never reach Settlement (FR-38), and a failed Settlement on a scored Type refunds the Builder (FR-35), so "fail Settlement … got nothing back" cannot occur. The qualifying clause admits only one reading; the metric owner should confirm it. |
| prose | §0 paragraph 2: "where it specifies field-level schemas (§1), the x402 wire contract (§3), and the Settlement computation (§4); … (§8)"; bare "addendum §3 / §6 / §1 / §2 / §8" at FR-2, FR-3, §4.4 description, §4.10 description, §7 Trust, §7 Compatibility, Open Question 1 | Write the four §0 pointers as "addendum §1", "addendum §3", "addendum §4", "addendum §8", and append to §0: "Below, 'addendum §N' means this file's addendum; the brief's addendum is always named 'brief addendum'." | Two addenda are inputs (frontmatter). Only §2.3 and FR-17 say which one they cite. §0's bare "(§1) … (§8)" sits in a document that has its own §1 to §8. |
| prose | §4.9 description and FR-43: "agent exchange log on the left" | "agent request/response log on the left" | "exchange" means Binance everywhere else (Platform Exchange Account, exchange credentials, exchange balance, exchange-side limit). |
| prose | §3 Settlement entry ends "(§4.8)." | Append: "Lower-case 'settlement' and 'settlement receipt' mean x402 payment settlement by the Facilitator, not this job." | The x402 Handshake entry, Facilitator entry, FR-16, FR-25 ("if the settlement is found on-chain"), §7 Run latency, and SM-1 use the lower-case word for payment settlement. The case distinction carries the meaning but is never stated. |
| prose | Open Question 2: "the Order Cap is the only capital ceiling in the demo and §8 says so." | "the Order Cap is the only capital ceiling in the demo; record that in §7 Security and safety." | §8 no longer holds the Safety bullet (moved to §7 by the structure pass), and neither section states this yet. "says so" reads as already written. |
| prose | FR-25 consequence 3: "A paid retry that times out is retried once with the same Payment Proof" | "A paid request that times out is resent once with the same Payment Proof" | "retry … retried" leaves the count ambiguous: one resend, or a retry of a retry. |
| prose | FR-17 consequence 3: "The minimal valid chains include `data, research, notify` and the full `data, research, risk, execution, notify`." | "Valid chains include the minimal `data, research, notify` and the full `data, research, risk, execution, notify`." | "minimal … chains include … the full" contradicts itself. |
| prose | FR-26 body: "amount, asset, network, or pay-to address" | "amount, asset, network, or payout wallet" | The Price Lock entry and FR-16 use "payout wallet"; this is the only "pay-to address". |
| prose | Defined-term case slips in §3 to §9: §3 System Wallet "receives refunds"; §4.3 description "refuses the listing"; FR-18 "the seed Agents"; FR-35 consequence 1 "The slash and the refund are"; FR-35 Notes "described the refund as partial"; FR-41 "slash tx hash"; FR-44 consequence 3 "if the live listing fails"; SM-1 "between run start" | "receives Refunds"; "refuses the Listing"; "the Seed Agents"; "The Slash and the Refund are"; "described the Refund as partial"; "Slash tx hash"; "if the live Listing fails"; "between Run start" | §3 defines these as capitalised nouns and the surrounding text uses them that way. The lower-case forms read as plain words; FR-44 has "seeded Listing" and "live listing" in one sentence. Verb forms ("slashes", "refunds", "lists") stay lower-case. |
| prose | §4.1 description: "the platform generates and holds every key, capped by the Daily Fee Budget on the spending side." | "the platform generates and holds every key; spending is capped by the Daily Fee Budget." | "capped" attaches to "every key". |
| prose | FR-7 Notes: "An Agent with Stake of ten Calls can be paid for a hundred inside one window; FR-25 reserves Stake against unscored Calls" | "Without FR-25, an Agent with Stake worth ten Calls could be paid for a hundred inside one window; FR-25 reserves Stake against unscored Calls" | Present-tense "can" states as current behaviour what FR-25's reservation check forbids. |
| prose | FR-35 consequence 4: "Each Call is settled at most once; the Settlement record is unique per Call and the window job skips Calls that already have one." | "Each Call is scored at most once; the Settlement record is unique per Call and the Settlement job skips Calls that already have one." | "settled" here means scored, colliding with the payment sense above; "window job" is a one-off name for the Settlement job. |
| prose | §7 Demo reliability: "The engine retries an RPC or network error up to three times before failing a Call, never re-signing a payment." | Consider: "The engine retries a chain RPC error up to three times before failing a Call, never re-signing a payment; a paid request to an Agent is resent once (FR-25)."? | "network error" also describes an Agent timeout, for which FR-25 allows one resend. Two counts for what may be one case. |
| prose | FR-25 consequence 4: "and skips payment with a failed Call if either blocks the order." | "and fails the Call before paying if either blocks the order." | Matches FR-32's "fails the Node before paying"; "skips" collides with the Call status `skipped`. |
| prose | FR-32 consequence 1: "fails the Node before paying with "no Telegram chat id linked" if it is missing." | "fails the Node before paying, with the reason "no Telegram chat id linked", if no chat id is linked." | "paying with" reads as a payment method; "it" is far from its antecedent. |
| prose | §8 Time: "Any FR not reachable by the end of day two on its stage is cut per §6, not deferred silently." | Consider: "Any FR whose §6 stage has not been reached by the end of day two is cut, not deferred silently."? | "on its stage" has no clear reading; the author should confirm which cut rule is meant. |
| prose | FR-13 consequence 1: "Reputation as a percentage with scored-call count, or "no score yet", or "not scored in MVP", plus owner address and status." | "Reputation (a percentage with scored-call count, "no score yet", or "not scored in MVP"), plus owner address and status." | The "or" chain reads as alternatives to the whole card. |
| prose | SM-1: "one Slash with Refund inside the three minutes, all without a manual step" | "one Slash with Refund, all inside the three minutes and without a manual step" | The time bound attaches to the last item only. |
| prose | §3 Call entry: "One invocation of an Agent inside a Run, …" | Consider: "One invocation of an Agent, normally inside a Run (the verification Call of FR-11 belongs to no Run), …"? | FR-11 defines a Call that is "not in any Run"; a strict reading of the glossary excludes it. |
| prose | §3 Registry entry: "holding price, Stake, Reputation, and status" | Consider: "holding the fields listed in FR-6"? | FR-6 lists Type, endpoint, price per call, Stake, Reputation, payout wallet, and status; the shorter glossary list reads as the full set. |

Further minor fixes, ask to expand: §2.1 "In the MVP runs are started by hand" reads as "MVP runs" until the verb (add a comma after "MVP"); §6 "Stage 1 and 2 run" → "Stages 1 and 2 run"; §5 "the MVP trades one Platform Exchange Account" → "trades through one Platform Exchange Account" (matches §4.7); mixed -ise/-ize ("authorisation", "centralised", "serialises", "Behaviour" against "finalized", "realized", "Realizes"); enum values are code-styled in FR-24 and FR-31 (`HOLD`, `REJECT`, `REJECTED`) but plain in FR-33 and FR-34 (LONG, SHORT, HOLD, APPROVE, REDUCE, REJECT); FR-34's per-Call "not scored" sits close to the per-Listing "not scored in MVP".
