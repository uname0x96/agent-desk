# Structure lens: PRD Addendum (`addendum.md`)

**Purpose read:** this document exists to help the architect and engineers build the engine, the Seed Agents, and third-party Agents against exact contracts (Type schemas, x402 wire, Settlement math), and to hand the pitch owner the demo-script corrections those contracts force.

**Structure model:** Reference/Database for §1–6 (random access, consistent per-item schema, MECE). §7 is a decision list and §8 an audience-specific appendix. The §8 mismatch is raised as a question rather than penalised, since the review context states it is a deliberate hand-off.

**Reader type:** humans. **Style guide:** Microsoft Writing Style Guide (configured default). **Length target:** none given.

**Word metrics** (`word_metrics.py`): 1,990 words total. Intro 56 · §1 399 (data 36, research 52, risk 55, execution 88, notify 139) · §2 253 · §3 286 · §4 250 · §5 191 · §6 107 · §7 108 · §8 269.

| Pass | Original Text | Revised Text | Changes |
|---|---|---|---|
| structure | §6 Environment modes — full section (107 words) | MOVE to sit between §3 and §4 (renumber: 6→4, 4→5, 5→6) | §4 says "production mode" and "demo mode" four times and §5 says "Demo Accounts" before either term is defined; the definitions arrive two sections later. A random-access reader landing on normative §4 has no anchor. Lighter alternative: keep the order and open §4 with "Modes are defined in §6." (0 words; +6 for the alternative) |
| structure | §4 "Risk rule (both modes)" — final sentence "Window prices from 1-minute klines in production mode and 1-second klines in demo mode." (14 words); §5 "Demo wallets" — "Demo Accounts carry a 100 USDT Daily Fee Budget." (8 words) | MERGE into the §6 table, where both rows already exist. Leave "Window prices at the kline granularity in §6." in §4; drop the §5 sentence | Same values live in two places; §6 is already the lookup table for every mode-dependent setting, so one owner prevents drift. Trade-off: §4 is normative and §6 is not declared so; if accepted, the intro must mark the §6 rows that §4 depends on as normative (saves ~15 words) |
| structure | Intro — "Sections 1, 3, and 4 are normative: they are acceptance criteria for the engine, the Seed Agents, and any third-party Agent." | QUESTION: is §2 meant to be normative? | The intro says the normative set covers the Seed Agents, but §2 is the only section that specifies Seed Agent behaviour (Guardrail thresholds, Alpha fallback, Sloppy's inversion), and §5's Stake-reservation bullet carries FR-25. Either add §2 (and that bullet) to the normative list or state that §2 is descriptive. Engineers reading only the declared set would skip testable rules (0 words; +4 if §2 is added) |
| structure | Intro — "Architecture reads this alongside the PRD and the brief addendum (…)" | QUESTION: extend the map with one sentence: "§2 and §5–7 are informative. §8 is for the pitch owner and engineers can skip it." | The intro tells the architect what to read and says nothing about §8, so the pitch owner has no signpost and engineers do not know they can stop at §7. Expectation-setting is cheap here (+15 words) |
| structure | §7 bullet 2 — "Facilitator path per §3: B402 on BSC testnet if partner access lands by day two; otherwise the fallback. Decide on day one and publish the active path." (27 words) | CONDENSE to "Facilitator path per §3: decide on day one." | §3 already states both paths, the day-two condition, and that the schema page publishes the active path. Only "decide on day one" is new (saves ~18 words) |
| structure | §1 `execution` — "On `HOLD` the engine skips `risk` and `execution`; on `REJECT` it skips `execution` (PRD FR-24). Skipped Nodes are never called." (22 words) | MOVE to the §1 opening paragraph, beside the other cross-Type rules (timestamps, amounts, unknown fields) | These are chain rules triggered by `research` and `risk` outputs, not part of the `execution` schema. A reader of the `research` subsection never learns that HOLD ends the chain. The `side` derivation sentence stays; it is an `execution` input rule (0 words) |
| structure | §8 Deviations from the brief's demo script — full section (269 words) | QUESTION: keep here, or fold the corrections into the brief addendum §9 and leave a pointer? | Scope: a PRD technical addendum carries a section for a non-engineering reader about a document it does not own. The review context says this is a deliberate hand-off, so the section is left intact; if the brief addendum will be updated anyway, this becomes a three-line pointer (0 words as-is; up to ~250 if folded) |
| structure | §2 closing line "Full chain cost with Alpha Research: 0.095 USDT. With Sloppy Research: 0.075 USDT." and §8 "Cost figures" bullet | PRESERVE both | Looks like duplication but serves two readers: §2 tells engineers what the preview must show, §8 tells the pitch owner what to say instead of the brief's 0.07 and 0.05. The §8 bullet is the correction itself; cutting either loses its audience (0 words) |

3 further minor fixes; ask to expand: (a) the `notify` verification-sample sentence sits better beside §3's verification-Call bullet; (b) §3's last bullet (reference middleware) is guidance, not contract, inside a normative section; (c) §3's nine bullets would scan faster grouped as request/response, payment, facilitator.

**Summary:** 8 recommendations (2 MOVE, 1 MERGE, 1 CONDENSE, 3 QUESTION, 1 PRESERVE). If every non-question row is accepted: about 33 words cut and 15 added, net ~18 words (≈1% of 1,990). No length target was set. The document is already tight; the value of these findings is ordering and single source of truth, not length. Comprehension trade-off: the MERGE makes normative §4 depend on §6's table, so the intro's normative map must extend to those rows. No reader aids are cut. Pacing is fine: JSON blocks, two tables, and bullet lists give enough visual variety.
